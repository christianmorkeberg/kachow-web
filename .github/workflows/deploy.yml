name: Deploy to Nordicway

on:
  push:
    branches: [master]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 33
          script: |
            cd /home/kachowdk/assistant.kachow.dk
            # Mirror origin exactly (resilient to force-pushes / history rewrites —
            # a plain `git pull` fails on divergence). .env & vendor are gitignored,
            # so they're left untouched.
            git fetch origin master
            git reset --hard origin/master